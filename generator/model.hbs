/**
 * @fileoverview {{inflect "titleize" (inflect "singularize" name)}} model
 */

//Imports
import {model as createModel, Document, Schema} from 'mongoose';

//{{inflect "titleize" (inflect "singularize" name)}} interface
export interface I{{inflect "classify" (inflect "singularize" name)}} extends Document
{
  {{#each (uniqueFields)}}
  /**
   * {{{description}}}
   */
  {{name}}: {{{typescriptType}}};
  {{#unless @last}}

  {{/unless}}
  {{/each}}
}

//{{inflect "titleize" (inflect "singularize" name)}} schema
const schema = new Schema<I{{inflect "classify" (inflect "singularize" name)}}>({
  {{#each (uniqueFields)}}
  {{name}}: {
    type: {{{mongooseType}}},
    required: {{required}}
  }{{#unless @last}},
  
  {{/unless}}
  {{/each}}

});

//Create the model
export const {{inflect "classify" (inflect "singularize" name)}} = createModel<I{{inflect "classify" (inflect "singularize" name)}}>('{{> camelCaseEntity}}', schema);